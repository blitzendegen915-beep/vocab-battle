# vocab-battle

高校英語の生徒向け「英単語バトルレート」Webアプリ。生徒が英単語クイズでレートを競う。

## 構成

- 純粋な HTML/CSS/JS（ビルド・フレームワーク・npm なし）
- `index.html` — 生徒/管理者の2タブ SPA。SheetJS（CDN）で Excel 読み込み
- `app.js` — 全ロジック（約1,500行）。設定・プレイヤー管理・クイズ・レート・ランキング
- `review.js` — 解答レビュー UI（自己完結の IIFE、スタイルも自前注入）
- `styles.css` — 全スタイル
- バックエンドは Google Apps Script WebApp。URL は `localStorage` の `gasWebAppUrl` に保存され、`fetch` で送受信する（GAS 側コードはこのリポジトリには無い）

## 個人情報の扱い（最重要）

生徒のクラス・出席番号・ニックネーム・暗証番号を扱うアプリである。

- 実装・テスト・動作確認では **必ずダミーデータのみ** を使う
- 実在の生徒名・番号をコード・コミット・スクリーンショットに含めない
- 収集する個人情報の項目を増やす変更は、指示があっても一度確認する

## 変更時の注意

- localStorage のキーは `STORAGE` 定数（app.js 冒頭）に集約されている。キーを増やす場合もここに追加する
- クライアント側データ形式を変えたら `CLIENT_DATA_VERSION` を上げて旧キャッシュを無効化する
- GAS 側 API の仕様変更を伴う修正は、GAS 側の対応も必要な旨を必ず報告する
- 既存 UI の見た目を壊さず機能追加する

## 動作確認

ブラウザで `index.html` を直接開く（サーバー不要）。GAS 未接続でも
localStorage キャッシュで画面遷移は確認できる。
